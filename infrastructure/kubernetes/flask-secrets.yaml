apiVersion: v1
kind: Secret
metadata:
  name: flask-db-secret
type: Opaque
data:
  # Intent: Passwords must be Base64 encoded in K8s secrets.
  # This isn't encryption, but it's a 'lock' that keeps eyes off plain text.
  mysql-password: cGFzc3dvcmQxMjM= # 'password123' in base64